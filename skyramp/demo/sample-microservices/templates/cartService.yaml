{{- if .Values.cartService.create }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.cartService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.cartService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.cartService.name }}
    spec:
      containers:
      - name: {{ .Values.cartService.name }}
        image: {{ .Values.cartService.image.repository }}:{{ .Values.cartService.image.tag }}
        ports:
        - containerPort:  {{ .Values.cartService.containerPort }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cartService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
  - port:  {{ .Values.cartService.servicePort}}
    name:  {{ .Values.cartService.name }}
    protocol: {{ .Values.cartService.protocol}} 
  selector:
    app: {{ .Values.cartService.name }}
{{- end }}

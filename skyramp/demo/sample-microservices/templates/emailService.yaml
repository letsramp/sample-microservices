{{- if .Values.emailService.create }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.emailService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.emailService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.emailService.name }}
    spec:
      containers:
      - name: {{ .Values.emailService.name }}
        image: {{ .Values.emailService.image.repository }}:{{ .Values.emailService.image.tag }}
        ports:
        - containerPort:  {{ .Values.emailService.containerPort }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.emailService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
  - port:  {{ .Values.emailService.servicePort}}
    name: {{ .Values.emailService.name }}
    protocol: {{ .Values.emailService.protocol}} 
  selector:
    app: {{ .Values.emailService.name }}
{{- end }}

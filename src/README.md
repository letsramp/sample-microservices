# Building services with docker compose

The sample services can all be built and pushed to a container registry with docker compose.


## Update container registry
```
open docker-compose.yaml and tename the host paty of the images with a docker registry you have access to
```


## Building Services
```
docker compose build

```


## Pushing Services
```
docker compose push

```
